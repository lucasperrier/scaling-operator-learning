# Steering Log

Append-only record of every action the `scaling-ops-steer` remote trigger
has executed on behalf of the user from their phone. Most recent entries
at the bottom.

(empty — no remote steering actions yet)
